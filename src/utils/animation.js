const initial = {
  scale: 0.8,
  opacity: 0,
};

const animate = {
  scale: 1,
  opacity: 1,
};

const transition = {
  duration: 0.4,
};

const viewPort = {
  once: false,
};

export { initial, animate, viewPort, transition };
